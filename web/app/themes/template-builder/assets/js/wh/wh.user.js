/**
 * wh.user
 * @module wh.ui.touch, wh.ui.mobile, wh.ui.viewport, wh.ui.resized
 * Describes device/browser properties and handles specific events
 */

;
(function(wh, undefined) {

    'use strict';

    // User related module
    wh.user = wh.user || {};


    /**
     * Description
     *
     * @method wh.user.
     * @param {Any} [args*] Arguments to be evaluated
     * @return {Boolean} True if user agent is in the list.
     **/
    wh.user.pin = function() {};

})(window.wh = window.wh || {});
